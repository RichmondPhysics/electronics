\section{The 555 Timer (and Friends)}
\label{lab_timers}

%\makelabheader %(Space for student name, etc., defined in master.tex)

\bigskip

If you were trapped on a desert island and could have only a single integrated circuit with you, what would you choose?  One good answer is the 555 timer, which consists of two comparators and one flip-flop, plus a few extra internal resistors and an output stage to increase its utility.  As the block diagram below suggests, 555 timers can do anything that flip-flops or comparators can do alone, but they often simplify your circuit by requiring fewer external components. 

\medskip
%\begin{center}
%\vspace{-0.05in}
%\includegraphics[width=3.25in]{timers/555_block_diagram.pdf}
\includegraphics[scale=0.96]{timers/555_block_diagram_multisim.pdf}
\hspace{-0.7in}
\hfill
\includegraphics[scale=0.72]{timers/lm555_simple_2line.pdf}
\hspace{-0.2in}\includegraphics[scale=0.72]{timers/ne556_abrev_2line.pdf}
\hspace{-0.1in}
%\vspace{-0.05in}
%\end{center}

\medskip
The 555 timer was first developed in 1971, and is still selling over a billion chips per year.  The original was the NE555 made by Signetics, but it is now made by many  manufacturers and in countless variations (for high speed, low power, low voltages, extreme temperatures, etc.).  It also comes in versions with two or even four devices on one chip.  The pinouts for two common versions are shown above.  In this lab, we'll consider three common uses for this best-selling chip of all time. 

\medskip
\textbf{Bistable Mode}
\medskip

When wired as shown below, the 555 acts like a simple flip-flop---without the fancy clock and data inputs that you get with the CD4013.  There's another difference between this and the CD4013: in Lab~\ref{lab_flipflops}, you \textit{raised} SET and RESET to change Q and $\mathrm{\overline{Q}}$.  Here, the polarity of the SET and RESET inputs are reversed, in that they are normally held high at $V_\text{CC}$ and are \textit{lowered} to change $V_\text{out}$. %The inputs are normally held high at $V_{CC}$.  When the trigger input is momentarily brought to ground, the output swings to $V_{CC}$ and stays there.  
%When the reset is momentarily brought to ground, the output swings to 0~V and stays there.  
%What happens when the reset is momentarily brought to ground?
(Note: although the CONTROL input is not specifically used in this application, it is common nevertheless to place a 10~nF capacitor between CONTROL and ground to reduce unwanted interference.)  

\begin{enumerate}[wide]
\item Build the circuit below, using logic switches for the two inputs and a logic indicator to view $V_\text{out}$.  Other than the reversed polarities of SET (``TRIGGER'') and RESET, does this flip-flop exactly follow the logic that you found in Lab~\ref{lab_flipflops}, part~\ref{part_SR_latch}---including when SET and RESET are used simultaneously?
\begin{center}
\includegraphics{timers/bistable_555.pdf}
\end{center}

\pagebreak[4]
\textbf{Monostable Mode}
\smallskip

In the circuit to the right, the 555 acts as a \textit{one-shot timer.}%
\footnote{The one-shot timer is something like the inverse of the ``delay'' circuit you built in Lab~\ref{lab_capacitors}, parts~\ref{part_delay_switch}~--~\ref{part_delay_switch_last}, on page~\pageref{part_delay_switch}.  There, changing the input caused the output to change only \textit{after} a given delay time.  Here, the output of the one-shot timer turns \textit{on} instantly, but remains on only for a given time before turning back \textit{off}.}
When a trigger signal is given, the output signal jumps from 0 to $V_\text{CC}$ for a time given by 
\figuretoright{0.38}{timers/monostable_555.pdf}{
$$\Delta t = \ln (3) RC \approx 1.1 RC$$
before resetting to zero and waiting for another trigger event.  While waiting for a trigger, the discharge pin (``DIS'' in the figure) is held at 0~V so the capacitor does not charge. After the trigger, the capacitor is charged through resistor $R$.  

\medskip
\item Build and test the circuit to the right, aiming for a delay time of $\Delta t \approx 3$ seconds. Note that as in the previous part, the trigger input here is normally held high at $V_\text{CC}$.  To start the one-shot timer, bring the trigger input low, and then \textit{immediately return it} to $V_\text{CC}$.  The timer will not work if the trigger remains low.
\label{part_one_shot_timer}  
}
%\begin{center}
%\includegraphics{timers/monostable_555.pdf}
%\end{center}

\item Looking at the block diagram of the 555 on the previous page, you can begin to see how the one-shot timer works.  The output is controlled by the flip-flop, whose R and S inputs come from the upper and lower comparators.  The S input is activated when the voltage at the TRIG input falls below a specific fraction of $V_\text{CC}$, and the R input is activated when the voltage at the THRESH input goes above a specific fraction of $V_\text{CC}$.  At what fractions of $V_\text{CC}$ are R and S activated? You should be able to tell by looking at the three 5~k$\Omega$ resistors in series that are connected to the other comparator inputs. 

\bigskip
\textbf{Astable mode}
\medskip

In this mode, the 555 acts as a square wave generator, much like the one you built using a single comparator in Lab~\ref{lab_capacitors}, part \ref{part_square_generator}.  In the circuit shown below, when the output swings high to $V_{CC}$, the capacitor $C$ charges through $R_1$ and $R_2$.  When the output swings low, the discharge lead (pin 7) is pulled to 0~V, and the capacitor discharges---but only through $R_2$.  Because of this difference, the square wave produced by this circuit is asymmetric, with the dwell times given by
\begin{align*}
\Delta t_{\rm high} &= \ln (2) C(R_1 + R_2) \textrm{, and} \\
\Delta t_{\rm low} &=\ln (2) CR_2.
\end{align*}

\item Build the circuit below, choosing values for $R_1$, $R_2$, and $C$ that will yield a square wave with a ratio of 
$\Delta t_{\rm high} / \Delta t_{\rm low} =2$, and a frequency of $f \approx 10$~kHz. \label{part_astable}
\begin{center}
\includegraphics{timers/astable_555.pdf}
\end{center}

\item The ``duty cycle'' of a square wave is the percentage of each cycle that the output is at a high voltage.  If you want to make a square wave with a duty cycle of less than 50\%, there's a neat trick you can use to make the capacitor charge faster than it discharges: place a diode in parallel with $R_2$, oriented so that $R_2$ is effectively shorted out during the charging phase.  Modify your previous circuit by adding a diode and changing one or both of the resistors so that the output pulse has a duty cycle of about 10\%.  (Don't worry about its frequency, just get the duty cycle close.)  Is your measured duty cycle exactly what you predicted?  If not, what might account for any discrepancy? \label{part_square_wave_duty_cycle}

\item Use a single 556 timer chip (two timers on the same IC) to build a beeper that beeps a warning at about 1~kHz.  The beeper should sound on and off about once per second, like a construction vehicle backing up.  To accomplish this, you'll want to have one 1~Hz oscillator that effectively turns a second 1~kHz oscillator on and off every second.  Looking at the circuit diagram in part~\ref{part_astable}, what's a good way to turn your oscillator on and off without cutting power to the entire chip?

\item You probably noticed that the pitch of your beeper sounds a little unsteady.  What's happening is that because of the low impedance of the speaker, the high current drawn from the 556 timer is causing some change in its performance, perhaps due to internal heating.  To fix this problem, use a single MJE3055T transistor as an emitter follower amplifier between the 556 timer and the speaker.  The pin-out diagram of the MJE3055T is shown in Appendix~\ref{appendix_pinouts}.  Draw a diagram showing how to wire this up, and build it to confirm that it works as expected.
\end{enumerate}







